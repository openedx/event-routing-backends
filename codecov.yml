coverage:
  status:
    project:
      default:
        enabled: yes
        target: auto
        # One-time coverage-measurement shift from this migration; see PR description.
    patch:
      default:
        enabled: yes
        target: 100%

comment: false
